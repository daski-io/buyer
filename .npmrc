# No install scripts anywhere in this tree: dependencies are chosen so none
# are needed, and a postinstall is exactly the hook a supply-chain attack
# wants. @napi-rs/keyring ships prebuilt binaries as optional dependencies.
ignore-scripts=true
save-exact=true
